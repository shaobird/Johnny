# Secrets — never commit
.env

# Runtime logs
*.log

# Runtime memory — never commit (use memory.template.json for structure reference)
memory.json

# MrktEdge runtime state — never commit
mrktedge_cookies.json
seen_mrktedge.json

# Training loop runtime data — never commit
training_log.json

# API usage log — never commit
usage_log.json

# Gmail runtime state — never commit
seen_emails.json

# Google OAuth tokens
token.pickle
credentials.json

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# OS
.DS_Store

# Library file manager — local storage, never commit
library_data/
