# Secrets — never commit
.env

# Runtime logs
*.log

# Runtime memory — never commit (use memory.template.json for structure reference)
memory.json

# MrktEdge runtime state — never commit
mrktedge_cookies.json
seen_mrktedge.json

# Training loop runtime data — never commit
training_log.json

# API usage log — never commit
usage_log.json

# Gmail runtime state — never commit
seen_emails.json

# Google OAuth tokens
token.pickle
credentials.json

# Peter's runtime data — never commit (see *.template.json for structure)
agents/peter/data/watchlist.json
agents/peter/data/couple.json

# Cloned upstream financial-services repo (reference only, do not commit)
peter/reference/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# OS
.DS_Store
